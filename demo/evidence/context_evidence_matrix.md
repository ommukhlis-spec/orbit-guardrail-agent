# Context Evidence Matrix

This matrix maps demo claims to public-safe evidence artifacts.

| Claim | Evidence artifact | Status |
|---|---|---|
| The agent can generate a reviewer-ready MR briefing. | `demo/sample_mr_comment.md` generated by `python -m app.main --mode mock --out demo/sample_mr_comment.md` | Implemented |
| The demo has a clear before/after architecture story. | `demo/invoice_app_before/` and `demo/invoice_app_after_bad_mr/` | Implemented |
| The bad MR bypasses the service layer. | `demo/mr_diff.md` and `demo/invoice_app_after_bad_mr/app/Http/Controllers/InvoiceController.php` | Implemented |
| The guardrail engine detects Controller → Model bypass. | `tests/test_guardrail_engine.py` and `tests/test_demo_scenario.py` | Implemented |
| The workflow produces run evidence. | `app/logs/run_*.json` locally and `demo/evidence/demo_run_log.redacted.json` for public-safe sample evidence | Implemented |
| GitLab MR comment posting path is prepared. | `app/adapters/gitlab_mr_adapter.py` and dry-run command | Implemented as dry-run |
| Final GitLab Orbit/Duo/AI Catalog workflow is complete. | Dedicated GitLab project / AI Catalog artifact | Pending provisioning |

## Current safe claim

The project currently demonstrates a deterministic local MVP plus GitLab API dry-run mode. Final Showcase compliance still requires the hackathon-provisioned GitLab Orbit/Duo/AI Catalog integration.
